# pnpm 11 shells out to `pnpm install` before every `pnpm run <script>`
# to check the lockfile is in sync. On Node 24 that spawn currently fails
# on this machine, so the pre-run check is turned off -- run
# `pnpm install` yourself after changing dependencies.
verify-deps-before-run=false
