{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // "chrome" is here for addInitScript callbacks (mock-backend.ts),
    // which run in the browser page, not the Node test runner.
    "types": ["node", "chrome"]
  },
  "include": ["e2e", "playwright.config.ts"]
}
